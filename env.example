# Get a free key at https://aistudio.google.com/apikey (no credit card needed).
# Required for the AI Gym Trainer chat and the AI meal-description nutrition estimator.
# The rest of the app (workouts, nutrition search, body tracking, offline rule-based
# coaching) works fully without this key — only those two AI features need it.
GEMINI_API_KEY=AIza...
